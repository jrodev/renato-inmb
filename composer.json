{
    "require": {
        "php": ">=5.5.0"
    },
    "scripts":{
        "post-install-cmd": [
            "ls",
            "pwd",
            "composer install -d=./application"
        ]
    }
}
